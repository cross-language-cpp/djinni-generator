requires_eq_interface = interface +c {
  some_method() : bool;
} requires(eq)

requires_ord_interface = interface +c {
  some_method() : bool;
} requires(ord)

requires_all_interface = interface +c {
  some_method() : bool;
} requires(eq, ord)