# record comment
my_record = record {
  # record property comment
  id: i32;
  info: string;
  store: set<string>;
  hash: map<string, i32>;
  const string_const: string = "Constants can be put here";
}
